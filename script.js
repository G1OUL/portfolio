const box=document.getElementById("petals");const glow=document.createElement("div");glow.id="cursorGlow";document.body.appendChild(glow);for(let i=0;i<34;i++){const p=document.createElement("span");p.style.left=Math.random()*100+"vw";p.style.animationDuration=7+Math.random()*10+"s";p.style.animationDelay=-Math.random()*12+"s";p.style.opacity=.25+Math.random()*.7;p.style.transform="scale("+(0.5+Math.random())+")";box.appendChild(p)}window.addEventListener("pointermove",e=>{glow.style.left=e.clientX+"px";glow.style.top=e.clientY+"px"});document.querySelectorAll(".glass").forEach(card=>card.addEventListener("pointermove",e=>{const r=card.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;card.style.transform="perspective(800px) rotateX("+(-y*5)+"deg) rotateY("+(x*5)+"deg) translateZ(8px)"}));document.querySelectorAll(".glass").forEach(card=>card.addEventListener("pointerleave",()=>card.style.transform=""));document.querySelectorAll('a[href^="#"]').forEach(a=>a.addEventListener("click",e=>{const t=document.querySelector(a.getAttribute("href"));if(t){e.preventDefault();t.scrollIntoView({behavior:"smooth"})}}));